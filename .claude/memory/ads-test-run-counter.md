---
name: ads-test-run-counter
description: Incrementing label for ad-test output files so runs never overwrite each other
metadata: 
  node_type: memory
  type: project
  originSessionId: 91c6a15d-f8e6-486f-8d8d-7aa9746830da
---

Files downloaded from an external Ads API test go into `ad-tests/` labeled with an incrementing run number so nothing is overwritten. The user assigns/confirms the number per run.

- Run **264**: 2026-07-01 (Chili Agency, instagram-feed-square). No output — all attempts failed (my key-quote bug, then edge compose HTTP 546 WORKER_RESOURCE_LIMIT).
- Run **265**: 2026-07-01 LIVE (job 70, company 78 WITH brand posts) — SUCCESS but LOW quality: generic AI illustration. `ad-tests/test265_instagram-square_InstagramFeedSquare.jpg`.
- **TEST job 264**: 2026-07-01 test server (key `cf_a03…`, new company 493, NO brand posts → empty brief) — SUCCESS, HIGH quality photographic composite. `ad-tests/testforge_job264_instagram-square.jpg`.
- Key finding: WITH `brandVisualBrief` (from ref posts) → illustration (worse); WITHOUT brief → photo composite (better, like old banner261). The brief is steering the image model toward graphic/illustration style.
- **TEST job 265**: 2026-07-01 test (company 493), Diego photo passed in `reference_images`. Result `ad-tests/testforge_job265_diego-square.jpg`: good brand composite BUT the reference PERSON (Diego) does NOT appear — model used the ref only as loose style/context, generated a generic hands+phone scene. Likely the regression the user noticed (before, the ref person was composited as the subject).
- **TEST job 266**: 2026-07-01 test (company 493), Diego ref, AFTER the protagonist fix. Result `ad-tests/testforge_job266_diego-square.jpg`: SUCCESS — person is now the PROTAGONIST in an on-theme UGC content-creator scene (ring light, phone tripod, mic). Fix validated. Caveat: exact Diego identity NOT preserved (model generated a different young man). Next refinement = identity fidelity.
- **TEST jobs 267-270**: Diego attempts that failed to feature him — 2 causes (my test-method errors, not the logic): (a) the Supabase edge CANNOT fetch images hosted on testforge.chili.pa (only chili.com.br works); (b) uploading the cutout via company-assets CONTAMINATED company 493 with the unfetchable testforge URL. Also: `_diego-ref.png` = `img-topo-diego.png` = hosted `ref-1.png` (all the same 110440-byte cutout).
- **TEST job 271**: Diego with a PERSON-CENTRIC theme (mentorship, fresh company 622) → SUCCESS: person is the on-theme protagonist (`ad-tests/testforge_job271_diego-mentoria-square.jpg`), close to Diego. KEY INSIGHT: the campaign THEME drives whether a person shows — "social media management" pushes toward tablets/devices and suppresses the person; mentorship/coaching themes feature the person naturally.
- Identity pixel-perfect (banner254/261 level) needs the HYBRID composite: paste the REAL person cutout as a foreground `<img>` over the AI-generated background (the old HTML-render path did this; compose/`generation_type:image` redraws the person and drifts).
- **REVERT (2026-07-01, commit 1ef1ea0)**: today's protagonist-classification + identity-lock changes DEGRADED person identity fidelity. Restored `agents-ads/index.ts` to `4f0aeea` and `api/v1/external/generate-ads.php` to `cd7caf8` — the 2026-06-29 (banner261-era) versions. Deployed to Supabase + both FTP servers. Job 272 confirmed: Diego featured with EXACT face preserved (even his maroon shirt), red brand scene, banner261 quality → `ad-tests/testforge_job272_banner261repro-square.jpg`. Lesson: the 06-29 compose preserves reference-person identity far better than the classifier approach; use `reference_image` (singular) to feature. NOTE: this revert also undid the brand_visual full-brief change (brief cap back to 2000) — re-apply separately if the n8n full-brief is needed.
- **ASYNC company-assets validated (2026-07-03):** new async brand-visual brief on `company-assets.php` — submit returned `202 brand_visual_status:"processing"` + job_id (company 87, test phone 5500000000266), poll `{api_key,phone}` returned `cached` with brief in ~15s. Worker fires on LIVE. See [[external-ads-api-test-procedure]].
- **LIVE job 75 (2026-07-03):** ad gen for Chili Agency (phone 5511943239843, company 69) AFTER the async-brief change → SUCCESS in ~56s, brand-red composite, async brief consumed fine by the ads pipeline. `ad-tests/live_job75_chili-async-square.jpg`. Minor pre-existing bug seen: subheadline leaked English prefix "Built for {audience}" (generate-ads.php ext_enrich_campaign_for_generation ~line 300) when caller sends target_audience but no subheadline.
- **TEST job 273 (2026-07-03):** same campaign on TEST server (testforge.chili.pa, key `cf_a03…`, phone 5511943239843 → existing company 478 slug `chili-digital-1`) → SUCCESS ~60s, photographic composite. `ad-tests/testforge_job273_chili-square.jpg`. Quality issues (pre-existing, not async): (a) image model BURNED English text into the bg ("Scene of Success / Digital Marketing Management") + overlapped the Chili logo; (b) same "Built for {audience}" English leak.
- **THEME-SCENE FIX validated (2026-07-07, commit 61d9c08):** problem — heroRef ads kept the SAME "creator + ring light + analytics" scene regardless of campaign theme (banners 325-328: finance/skincare still showed a content creator). Root cause: `agents-ads/index.ts buildBackgroundPrompt` hardcoded the TikTok/ring-light example as THE concrete scene in 4 places (company/hero/service/themeLock), so non-content themes had only abstract guidance and reused the ring-light magnet + the ref image's own creator background. Fix: derive a concrete "scene of success" from the campaign topic (1x `gemini-2.5-flash` call per compose job, ~220 tok) → inject as `themeScene` param into buildBackgroundPrompt; heroRef now has a SCENE OVERRIDE (keep only the person's identity from the ref, rebuild surroundings as the derived scene); removed the hardcoded ring-light example everywhere + added an explicit anti-default negative (no ring-light/filming unless topic is literally content creation).
- **LIVE job 122 (2026-07-07):** skincare test, phone `+5511943239843` (WITH +) → created company 78 (NOT the real Chili). Google avatar (woman w/ glasses, paisley top) as `reference_image`. Result `ad-tests/live_job122_skincare-ref-square.jpg`: SUCCESS — full skincare bathroom scene (vanity, plants, serum bottles), theme perfectly adapted; identity partial (paisley top kept, glasses dropped, face drift — ref was only 193px).
- **LIVE job 124 (2026-07-07):** same skincare campaign, phone `5511943239843` (NO +) → real Chili company 69 (with brand posts). Result `ad-tests/live_job124_skincare-ref-realco-square.jpg`: BEST — person placed in a skincare bathroom holding the product, **glasses + paisley top + face preserved** (identity much better with the real company/brand context), Chili logo + red brand shapes. Theme adaptation + identity both working. KEY: use phone WITHOUT `+` to hit the existing company (69); with `+` it made a fresh company (78) that lacks brand posts → weaker identity.
- **LIVE job 128 (2026-07-07):** OTHER-THEME test — physical object (Pexels running sneaker) as `product_image_url` + running/fitness theme, Chili company 69. Result `ad-tests/live_job128_sneaker-fitness-square.jpg`: theme adapted (forest running trail, two runners) BUT the PRODUCT (sneaker) was ABSENT — the person-oriented themeScene derivation produced a lifestyle scene that swallowed the object.
- **Product-aware scene fix (2026-07-07, commit cd442d5):** when `hasProductRef` (object hero), the themeScene derivation now uses a product-specific system prompt: FEATURE the product large + in focus, build only the topic's environment/props around it, person may appear only partially. Redeployed agents-ads.
- **LIVE job 129 (2026-07-07):** same sneaker/running test after the fix. Result `ad-tests/live_job129_sneaker-fitness-fix-square.jpg`: SUCCESS — red running shoe is the sharp hero on a running track (curved lanes), theme adapted AND product featured. Notes: product re-colored to Chili red (colour-source lock re-grades product into brand palette) and model added a Nike-like swoosh — future refinement if we want to preserve the product's real color/shape.
- **job_id is the LIVE server's GLOBAL auto-increment (shared by all API callers + the app), NOT a per-me counter — gaps (e.g. 124→128) are other jobs in between, expected. File naming `live_job<id>` matches the real id to avoid overwrite.**
- **Verified public ref-image sources for tests (fetchable by the edge): Google avatar URLs (lh3.googleusercontent.com) and Pexels `images.pexels.com/photos/...jpeg` URLs both work. Use Pexels API (PEXELS_API_KEY in .env) to source object images; avoid packaging with heavy printed text/logos (fights the zero-text rule).**
- **Ready-to-run test examples prepared (not yet run): watch→luxury/elegance (Pexels id 6157408), coffee bag→café (has text on bag, weaker), person-ref→finance/fitness with the Google avatar. Physical-object refs go in `product_image_url`; person refs go in `reference_image` (composeHeroRef/heroRef).**
- **LIVE jobs 130/131/132 (2026-07-07):** three-theme batch, Chili company 69. 130 = watch→luxury via `product_image_url` (`live_job130_watch-luxo-square.jpg`): elegant watch hero on wood, city bokeh — theme adapted, product featured (dial blanked by zero-text rule). 131 = finance→person via `reference_image` (`live_job131_finance-person-square.jpg`): ref woman (glasses+paisley preserved) in a bright KITCHEN with documents/pen organizing bills — NOT a ring light (fixes the 326/327 failure). 132 = fitness→person (`live_job132_fitness-person-square.jpg`): same person in a GYM (dumbbells, treadmills) holding a weight. All three: theme adapted + identity preserved.
- **BRAND-POSTS PRESENCE CONFIRMED (2026-07-07):** user worried the brand-posts ref was missing in the new tests. Verified via read-only poll `POST company-assets.php {api_key,phone:5511943239843}` → company 69 has `brand_posts_stored: 20`, `brand_visual_status: cached`, `reference_images: 0`. Code trace: worker (`generate-ads-worker.php:608-644`) injects stored brand posts into composeCompanyRefs keeping the hero ref first (slice 0,10); edge takes composeCompanyRefs.slice(0,3) then bgRefImages. Person tests → model gets [person + 2 brand posts]; product tests (briefDriven) → edge reserves a slot: [2 brand posts + product]. So brand posts ARE reaching the image model (2 imgs) + the cached brand brief text, in every test. Added a `[refs]` diagnostic log to agents-ads compose (commit after cd442d5) that prints composeCompanyRefs/brandPostsInBg/genRefsInBg per job. NOTE: `supabase functions logs` is NOT available in CLI 2.109.1 — read edge logs via the Supabase dashboard or Management API instead.
- **LIVE job 133 (2026-07-07):** TikTok/viral theme, `reference_image` = a Bing thumbnail of hands holding a phone showing the TikTok app (no face), Chili company 69. Result `ad-tests/live_job133_tiktok-ref-square.jpg`. THEME CORRECT — content-creator studio scene (mic + pop filter, monitors with audio waveforms + rising analytics chart, LED lighting, Chili tee). Confirms the theme-scene fix does NOT over-correct: for a genuinely content/social topic it KEEPS the content-creation scene, while finance/skincare/fitness get their own scenes. Ref had no person → model invented a fitting creator (correct). DEFECT (pre-existing, NOT from the scene change): the image model BURNED fake gibberish body text into the dark lower background ("C stemi em dociabato…sem aulio"), visible behind the real subheadline — the zero-text rule fails more on dark/studio scenes (cf. job 273 English burn-in). Candidate next fix: strengthen zero-text enforcement / avoid dark empty text-zones that invite burned text.
- **heroRef-on-non-person BUG + fix (2026-07-07, commit after the [refs] log):** `reference_image` in the external API ALWAYS sets composeHeroRef=true (generate-ads.php:387) → edge heroRef "feature THIS person" mode. When the ref has NO person (a phone/app screenshot, product, scene, logo), the model featured a person pulled from the brand posts instead → the caller's ref had ZERO influence (user: "usou uma pessoa do brand post"). The SCENE OVERRIDE added earlier made it worse for non-person refs. FIX: vision-gate in compose — classify companyRefImages[0] via gemini-2.5-flash ("does this image feature a real person? YES/NO"); if NO → heroRef=false, bgSource='reference', bgRefImages=[that ref only] (drop brand posts) so the ref's real content is reproduced. Person refs classify YES → unchanged.
- **LIVE job 134 (2026-07-07):** TikTok, SAME Bing ref, AFTER the gate. `live_job134_tiktok-ref-gated-square.jpg`: STILL an invented person + brand-post Chili-heart studio → the Bing ref was NOT fetched by the edge (so companyRefImages[0] = a brand post w/ person → gate saw a person → no switch). Burned gibberish text gone though.
- **LIVE job 135 (2026-07-07) — PROOF:** same TikTok campaign but ref = a PEXELS phone/Instagram image (`images.pexels.com/photos/238480`). `live_job135_tiktok-pexelsref-square.jpg`: FAITHFUL reproduction of the phone-in-hand ref (apps on screen, finger, wood table, glass) — NO brand-post person. Controlled A/B (Bing vs Pexels, same everything) proves: (a) the vision-gate works, (b) the Bing failure was FETCHABILITY.
- **KEY: Supabase edge canNOT fetch `tse*.mm.bing.net` (Bing thumbnails) — datacenter IP blocked. Fetchable ref hosts confirmed: Pexels (`images.pexels.com`), Google avatars (`lh3.googleusercontent.com`), and forge.chili.pa (brand posts mirror). For arbitrary caller ref URLs to always work, a future fix would mirror `reference_image` to the Chili server (PHP-side, like brand posts) before the edge call — the Hostinger PHP host CAN fetch Bing; the edge cannot.**
- **Non-person-ref BARE-PASTE bug + fix (2026-07-07):** the first gate version, on a non-person ref, set bgSource='reference' + bgRefImages=[ref only] → the edge's reference SHORT-path just reproduced the raw ref photo full-bleed, dropping brand posts/colors/brief (user: "ref da empresa sumindo, imagem colada no fundo", job 135). FIX: on a non-person ref now set `refAsSubject=true` (→ passed as productRef=true) + bgSource='inspired' (long path) and KEEP bgRefImages=[ref, brand posts…]. So the ref is FEATURED as the hero subject (productMoodHint, ref is first) WITH brand identity (brief + colorLine + brand-post style refs). Also suppress themeScene for refAsSubject (it was derived on the person branch before the gate → would reintroduce a person). Person-ref and normal product_image_url paths unchanged.
- **LIVE job 136 (2026-07-07) — non-person-ref fix validated:** same Pexels phone ref. `live_job136_tiktok-ref-brandkept-square.jpg`: hand+phone featured as hero, composed into a Chili-branded content desk (red color grade, lens/tripod/plant, red particle accents, logo) — NOT a bare paste (vs job 135) and NOT a brand-post person (vs job 134). Both complaints resolved.
- **Recap of the reference_image logic now:** person in ref → heroRef (feature the person in the theme scene). Non-person in ref (product/phone/screenshot/scene) → featured as SUBJECT in a brand-styled scene, brand posts kept. Ref MUST be on a fetchable host (Pexels/Google/forge.chili.pa); Bing thumbnails fail silently → falls back to a brand-post person.
- **Compose OVERLAY (HTML text layer) work (2026-07-07):** the text/logo/CTA overlay HTML is built in `agents-ads/index.ts` (~line 500-650, the function with the "expert HTML/CSS advertising compositor" SYSTEM). Gradient highlight CHIP on a headline word = `agents-ads/index.ts:608` (the `<span background:linear-gradient…box-decoration-break:clone>` — the look the user likes). Style varies by `OVERLAY_STYLE_VARIANTS` rotated by jobId. Changes made: (a) CTA CONTRAST hard rule — CTA text must be legible vs what's behind it; default white, accent only on underline/arrow/one word, never the whole CTA text in the background's own hue (fixed "Saiba mais" red-on-red). (b) accent repertoire expanded (chip favored + highlighter sweep + marker underline + outline word). (c) subheadline legibility backing (soft fade gradient, never a pill/bar, never behind the CTA). (d) CTA arrow bigger/coloured, own line. Commits: 4dd85cd (CTA contrast), + overlay polish commit.
- **LIVE jobs 137-140 (2026-07-07) overlay validation:** 137 red/office CTA now white+red arrow (legible). 138 skincare — overlay good BUT background came out office/analytics not bathroom (brand-post pull / model variance — theme miss, NOT overlay). 139 digital — good accents but a light rounded strip leaked BEHIND the CTA (looked like a button) + a doubled/burned logo top-left (pre-existing bg burn). 140 (after tightening the subheadline-backing rule) CLEAN: CTA "Confira" white + coral underline, no container, single clean logo, legible subheadline. `ad-tests/live_job14{0}_overlay-*.jpg`.
- **Open follow-ups noted (not done):** (1) intermittent theme miss when brand=marketing-agency and theme=other (brand posts sometimes override the derived themeScene); (2) image model occasionally BURNS a logo/text into the dark/busy background → doubled logo with the composited one. Both pre-existing, separate from the overlay.
- **DIVERSE-THEME batch (2026-07-07, LIVE jobs 141-146):** 6 themes, each a FRESH fictional company (distinct fake phone → no brand posts, no brand colours/logo → bgSource resolves to 'creative'). Files `ad-tests/live_job14{1-6}_theme-*.jpg`. Result pattern: PERSON-centric themes render literal & great — imovel/142 (man in modern apartment), odonto/144 (girl smiling, white teeth, clinic), fitness/146 (man in a gym) all ✅ literal + clean overlay. PRODUCT/FOOD/ANIMAL themes WITHOUT a ref went ABSTRACT instead of showing the real thing — pizza/141 (abstract copper plates, no pizza), moda/143 (abstract fabric + BURNED TEXT 'Aurora Moda'+'spotify'), pet/145 (abstract shapes + tiny paw icon, no dog). Overlay itself held up on all (CTA contrast, accent word, chip/underline, legibility). 
- **Finding:** the no-ref/no-brand-post 'creative' bgSource lets object/food/animal themes drift to abstract; a real customer with brand posts (e.g. Chili co.69) is pulled to photographic, so this mainly bites brand-new empty companies. Candidate fix: when bgSource='creative' AND the topic is a tangible product/food/animal, force a LITERAL photographic hero of that thing (not abstract/conceptual). Burned-text on abstract bg (company name/random words) is the same pre-existing zero-text-fails-on-empty-bg issue. NOT yet fixed — offered to the user.
- **User preference (2026-07-07):** for "temas diversos" tests, ALWAYS keep the EXISTING company (phone 5511943239843 → Chili co.69) — a fresh company has no logo and no brand posts, so scenes go abstract/logoless (bad test). Real tests use co.69 so logo + 20 brand posts drive a photographic look.
- **REF-MODE batch on co.69 (LIVE jobs 153-155):** all 3 paths work with brand identity. 153 no-ref → e-commerce warehouse scene (brand posts → photographic, not abstract) `live_job153_refmode-noref`. 154 person ref (Google avatar) → woman featured in a marketing-office scene `live_job154_refmode-pessoa` (DEFECT: Chili logo bottom-left collided with the CTA). 155 product ref (Pexels watch) → watch featured as hero WITH Chili red panel+dots+logo (refAsSubject working, not a bare paste) `live_job155_refmode-produto`.
- **DIVERSE themes on co.69 (LIVE jobs 147-152):** far better than the empty-company batch (141-146) — brand posts pull to LITERAL photographic scenes. imovel/148, odonto/150 (smiling man, white teeth), fitness/152 (gym), moda/149 (man in knitwear, cozy cafe) all ✅ literal. pizza/147 = restaurant scene ✅ but BURNED TEXT in bg ("Wir quality fo[r]s mediterranean…"). pet/151 = ❌ became a home-office scene, NO dog (the brand-pull-to-office miss, same as skincare/138). Files `live_job14{7-52}_theme-chili-*`.
- **3 RECURRING DEFECTS to fix next (across all batches):** (1) brand-pull-to-office — themes far from marketing (pet, skincare) sometimes let brand posts override the derived themeScene → generic office without the actual subject; strengthen theme-subject priority over brand posts. (2) burned text in background (company name / random words like "spotify") — zero-text rule fails intermittently on busy/scene backgrounds. (3) logo↔element collision — logo occasionally touches the CTA (job 154) despite the no-overlap rule. All offered to the user, not yet fixed.
- **User insight (2026-07-07):** advertising a service far from the REGISTERED company's own domain (e.g. Chili = marketing agency running a pet-grooming/pizza ad) doesn't work well — brand posts pull the scene back to the company's real domain (the "brand-pull-to-office" miss). This is largely a TEST artifact: in real usage each company advertises its OWN services, so company↔theme match and the conflict rarely arises. So defect #1 (brand-pull) is LOWER priority than #2 (burned text) and #3 (logo↔CTA collision), which happen even on matched company/theme. User will next test via a real app using the API and share results here.
- **BURNED-TEXT fix (2026-07-07, real app result):** user's app produced a ROI/marketing ad (`ad-tests/WhatsApp Image 2026-07-07 at 11.34.46.jpeg`) with burned bg text: a fake 'Learn More' red button, 'Digital Media Specialist / Hon-Upcto' text (likely traced from a Chili brand post), and a tablet dashboard with numbers/labels. Note: the bg text-detector (`backgroundHasText`, gemini-3-pro, wired into compose at ~3959 with retry→shapes-fallback for non-hero, best-of-3 for hero) ALREADY exists but this leaked (detector false-neg on faint text, or hero best-of-3). Prompt+detection were already near-maxed. FIX: added a CONCRETE ban to the zero-text rule naming the exact recurring offenders — fake CTA buttons ('Learn More/Shop Now/Confira/Saiba mais') and charts/dashboards with numbers/percentages/axis-labels (must be pure abstract bars). Commit after c261173. Validated job 160 (ROI theme on Chili): fake button + 'Digital Media Specialist' + chart numbers ALL gone.
- **Remaining tail (job 160):** a decorative brand-like GRAPHIC still leaked — a red megaphone + speech-bubbles + 'if' monogram drawn on a glass wall (marketing-theme icon the model invents as decoration). Text is fixed; logo-like ICON/graphic marks are a separate harder tail the text-detector doesn't reliably catch. Candidate next: strengthen `backgroundHasText` to flag icon/emblem/mascot graphics, and/or explicitly ban theme-motif icons (megaphone, speech bubble, rocket, lightbulb) in the bg prompt.
- **ALWAYS-PEXELS CONTEXT GROUNDING (2026-07-07, big win):** user insight — redoing an ad WITH a reference image almost never burns text/icons, because a real photo makes the model REPRODUCE reality instead of INVENTING. So: when the external API sends NO ref, always fetch a context-relevant Pexels photo and use it as the hero ref. Before: the no-ref block only fetched Pexels on `jobId%2` (half the jobs) with a generic 'happy person portrait' query. Now: ALWAYS fetches, and derives a CONTEXT query via `pexelsQueryForCampaign` (gemini-2.5-flash → 'person holding smartphone' / 'barista pouring coffee' / etc. from campaignFactsImg). Vision-gate guarded with `!pexelsHero` so it doesn't classify a brand post and drop the Pexels photo. Fallback = invent-in-prompt if Pexels misses. Commit after 54a1670. Validated: job 162 (ROI/marketing — the exact scenario that burned a fake 'Learn More' button + 'Digital Media Specialist' + megaphone icon in job 160) now GROUNDED in a real office photo, screens off, ZERO burned text/icons; job 163 (coffee) — context query fetched a real coffee scene (mug, Chemex, beans), not a generic portrait. `ad-tests/live_job16{2,3}_pexground-*`.
- **KEY LEVER for burned-text/icon defects: grounding in a real reference photo beats any prompt/detector tweak.** Prefer Pexels-grounding for no-ref generations. The invented-scene path is what hallucinates fake UI/text/logos.
- **Pexels hero UNIFIED with caller reference_image (2026-07-07):** user asked that a Pexels-grounded hero behave EXACTLY like a caller-sent reference_image (brand posts were pesando menos with Pexels). Root cause was a special-case block: the Pexels image was prepended to bgRefImages AFTER the fact and reset `brandRefCountInBg=0` + the vision-gate was skipped (`!pexelsHero`). FIX: the Pexels image is now the FIRST element of `companyRefImages` (the genRef slot), so bgRefImages build + vision-gate + brand-post weighting run identically to a real reference. Removed the special pexelsHero block and the gate guard. Validated jobs 164 (ROI) / 165 (coffee): grounded + clean + brand applied with caller-ref-equal weight. IMPORTANT framing for the user: any residual brand-presence difference is INHERENT to the image (a full stock photo is a complete scene → limited room for brand to reshape it), NOT a code penalty anymore. Commit after 5ecfb80.
- **castingRef — Pexels hero RE-STAGED into a brand scene (2026-07-07):** user: Pexels no-ref ads still looked like 'stock photo + logo' and diverged from the 'designed/branded' look of caller-ref ads. Root cause: a full stock photo is a complete scene → model reproduces it → weak brand integration; a caller cutout/asset forces the model to BUILD a brand scene → looks designed. FIX: added `castingRef` param to buildBackgroundPrompt (true only when pexelsHero). In casting mode the heroRef block flips from 'preserve exact identity + reproduce' to 'similar person, do NOT reproduce the stock background, RE-STAGE into a fresh brand-designed scene with strong brand colour grade + ONE brand device + negative space'. Caller reference_image keeps strict identity (castingRef off). Validated jobs 167 (travel) / 168 (ROI): brand now DOMINATES (strong red grade, dot-grid device integrated), looks designed, still grounded (real people → no burned text). Commit after 0888278. Caveat: red grade can get quite strong (job 167 beach nearly saturated) — dial back if the user finds it heavy.
- **castingRef topic-tie (2026-07-07):** user noticed the re-staged Pexels person was in a generic branded scene (just sitting/smiling in a red office) — 'não tá no assunto do ads', lost the topic connection. FIX: the castingRef re-stage instruction now ties to the THEME SCENE — the person must be ACTIVELY DOING the campaign's activity (props/setting of the topic), explicitly banning 'generic person just sitting in a plain office'. Validated job 169 (ROI → woman presenting an abstract data chart on a tablet, on-topic) and 170 (bakery → baker kneading dough, perfect). Commit after de79539. REMAINING: job 169 had a doubled/burned 'Chili' wordmark top-left (the logo/icon burn tail) — re-staging's extra invention slightly raises the logo-burn temptation and the text-detector missed it. Padaria/170 was clean. Logo/icon burn is still the open tail.
- **Pexels-as-ref = the winning approach (2026-07-07):** user confirmed 'ficou perfeito' when a Pexels image is sent AS a `reference_image` (job 172, marketing service: person re-lit + strong Chili red blobs/dot-grid, clean, no burned text). So castingRef was REVERTED — the Pexels auto-hero now gets the SAME strict reference_image treatment (heroRef, reproduce, no re-staging). castingRef branches left dormant in buildBackgroundPrompt (param defaults false, no longer passed).
- **Pexels opt-out added:** `compose_background_source: 'creative'` (or 'shapes') now SKIPS the auto-Pexels fetch → the model invents/abstracts the scene (no-ref-no-pexels path). Commit after 5d49b90.
- **LIVE jobs 172-174 (marketing service 'gestão de redes', co.69):** 172 Pexels-AS-ref → ✅ strong branding, clean (`live_job172_pexels-AS-ref`). 173 auto-Pexels no-casting → ❌ MESS: burned brand-post caption ('Why instant results don''t exist in Digital Marketing / THE TRUTH NO ONE TELLS YOU'), red marketing icons (megaphone/thumbs/speech), doubled logo (`live_job173_pexels-auto-nocast`) — a bad auto roll: marketing theme + text-heavy brand posts → the model echoed a caption. 174 no-Pexels invent (`creative`) → ✅ clean professional office meeting, no burned content, subtle red branding (`live_job174_noref-nopexels`). Takeaway: hand-picked/curated ref >> auto-Pexels for branding+cleanliness; the invent path is surprisingly clean here; auto-Pexels on text-heavy-brand-post companies can still echo captions.
- **Burned-content 'novidades' fixed (2026-07-07):** two targeted fixes for the job-173 failure class. (1) ANTI-ECHO: `REF_TEXT_LEAK_GUARD` (a strong 'do not trace text/captions/logos from the attached reference/brand-post images' guard) was DEFINED at index.ts:299 but NEVER used — now appended to the bg prompt whenever `hasRefImages` (brand posts / refs attached). Kills the traced brand-post caption. (2) ICON/LOGO DETECTION: `backgroundHasText` VSYS+VQ now also flag drawn ICON/PICTOGRAM graphics (megaphone, speech/chat bubble, thumbs-up, heart, star, badge, envelope, rocket, lightbulb, hashtag, play) and a DUPLICATE/doubled logo — not just text; the NO_TEXT_RETRY_REMINDER also bans drawn icons. Validated jobs 175/176 (auto-Pexels 'gestão de redes', the exact 173 scenario): both clean — no burned caption, no marketing icons, no doubled logo (job 173 had all three). Commit after 2b3e49c. Minor nits seen (not the target defects): job 176 overlay accent sampled GREEN from the chart instead of brand red; job 175 TV had very faint label-ish marks.
- **Pexels now sent as a REAL reference_image (2026-07-07):** user insisted the auto-Pexels image be sent 'da MESMA maneira que qualquer outra imagem de referência, assim como o Diego'. Old auto path downloaded the photo as base64 and injected it (pexelsHero) — a parallel path. NEW: `fetchPexelsUrl` returns the Pexels image URL (no download); the auto block sets `campaignData.referenceImageUrl = url`, `composeHeroRef = true`, and puts the URL FIRST in `composeCompanyRefs` — so the edge fetches it via the normal `fetchImageAsBase64` as `companyRefImages[0]`, identical to a caller ref. Removed the base64 `pexelsHero` mechanism entirely (fetchPexelsPerson now unused/dead). Validated jobs 177/178 ('gestão de redes' no-ref): both clean + branded (red grade, Chili blobs/dot-grid, single logo, no burned text/icons). Commit after 3168111. NOTE: functionally near-identical to the prior unified path (same companyRefImages[0] = the Pexels photo), but now the code genuinely routes it through the reference_image shape. The bigger quality lever remains the IMAGE the query picks (busy office scenes → more clutter risk); could improve `pexelsQueryForCampaign` to prefer cleaner subject shots if needed.
- **Burned LAYOUT-MOCKUP text + brand-device suppression (2026-07-07):** two real-app failures + one regression. (a) Model drew a 'logo' PLACEHOLDER box + a title card with the service name ('SEO and Performance Media') — treating the gen as a layout mockup. (b) Model burned the literal CSS coordinates from the prompt ('logo[top:5%;left:5%...]' 'text-block[...]') into the bg — traced from buildBackgroundPrompt's TEXT-SAFE ZONE line (2650) + reference short-path (2371). FIXES: added an anti-mockup line to PRIMARY_BG_RULE (no logo placeholder, no 'logo' word, no service-name title card); replaced the literal `logo[${pos}]`/`text-block[${pos}]` coordinate strings with plain-word descriptions. REGRESSION the user caught: the first anti-card wording ('no cards/chips/badges/panels') over-suppressed the brand's OWN devices (red blobs, dot-grid) → 'as ref de posts da empresa sumiram'. Re-worded: ban only cards/panels that CONTAIN text/logo; explicitly ALLOW blank brand-colour shapes/blobs/dot-clusters/panels. Validated: 182/183 (SEO) no more placeholder/title cards; 185/186 (Midia IA) no burned coordinates and brand dot-grid device restored (186). Commit after 16d1698 (two commits).
- **TEXT-ELIMINATION safety net (2026-07-08):** root gap — since every no-ref gen is now heroRef (Pexels sent as ref), the bg-text detector's shapes fallback NEVER fired (heroRef protects the subject) so persistent-text cases shipped 'best-of-3' WITH text (the .jfif with burned CSS coords, the SEO title cards). FIX in the standard-path retry (`agents-ads/index.ts` ~4062): when the hero is AUTO-Pexels (`pexelsAuto` flag, an expendable stock person) and all 3 photographic attempts still leak text, fall back to an ABSTRACT shapes background (cannot contain legible text) → guarantees a text-free ad. A real caller ref (Diego, heroRef && !pexelsAuto) still keeps best-of-3 (protects the chosen subject). Also re-enabled castingRef ONLY for pexelsAuto (caller ref stays strict) so stock heroes are re-staged creatively in the brand-post style. Validated jobs 203-205 (Midia IA no-ref): all clean (no burned text) AND creative + brand-aligned (Chili blobs/dot-grid/red panels, dynamic, on-topic) — none needed the fallback, casting reduced the mockup/text tendency at the source. Commits after 408000a: 572a4df (casting for pexelsAuto) + the shapes-fallback commit.
- **Caller-ref path: brand-posts weak + burned text (2026-07-08):** user's real API gen (`WhatsApp Image 2026-07-08 at 09.49.33.jpeg`) used the Bing woman as `reference_image` (caller ref, pexelsAuto=false) → (1) burned a traced brand-post caption ('growth, conversion e ROI') + dashboards with numbers (best-of-3 shipped because caller-ref has no shapes fallback); (2) NO brand-post devices (dots/shapes) — because the strong 'study brand posts' instruction was castingRef-only, and caller ref is strict. DEBUG (job 210) confirmed brand posts ARE sent: `bgRefImagesSent:3` = [pexels-hero, brand-post-22, brand-post-23], 11 brand posts in composeCompanyRefs, briefDriven — so they're NOT absent, just weakly reflected. FIXES (commit 848971c): (A) the heroRef brand-post line now tells the model to STUDY the posts and ECHO their signature DEVICES (shapes/dots/panels/motifs) into empty areas — applies to strict caller-ref too (not just casting). (B) the final anti-text retry now drops the brand-post images (`bgRefImages.slice(0,1)`, hero only) — removes the traced-caption source while keeping the person, so caller refs can come out clean WITH the subject.
- **⚠️ GEMINI_API_KEY_PRODUCTION CREDIT DEPLETED (2026-07-08):** after ~200+ test generations this session, the production key hit HTTP 402 `gemini_credits_depleted` ('Recarregue/ative o billing em ai.studio'). Fixes A/B are DEPLOYED but NOT yet validated — need the key recharged first. Bing ref (tse1.mm.bing.net) DID fetch successfully in jobs 207/WhatsApp (contrary to the earlier job-134 note — Bing fetchability is intermittent, not always blocked). Testing burns real money (~$0.10-0.13/ad) — be economical.
- **Cohesion + brand-colour lock on 2.5-flash-image (2026-07-08):** job 225 (genius ref + marketing theme) got cohesion right (person integrated into a real marketing-office scene) BUT graded the whole ad TEAL — it copied the ref model's teal blazer instead of the brand palette. FIX (commit after 225): the heroRef cohesion rule now says the person matches the scene's LIGHT/PERSPECTIVE but the WHOLE image is graded in the BRAND's palette (never the ref's clothing/incidental colour). Job 226 (same ref+theme) confirmed: teal gone, palette back to brand navy+orange, blazer re-graded to navy, cohesion held. Mild burned chart/bars still appear on screen/dashboard themes with 2.5 (accepted — client regenerates). Costs: 225=$0.054, 226=$0.046.
- **Brand colour must come from the BRAND-POSTS, not stored CSS-var hex (2026-07-08):** job 226 graded the ad navy+orange because that's the company's stored brand CSS-var, but company 69's real brand posts are RED — the stored hex was stale/wrong. FIX: `buildBackgroundPrompt` now takes `hasBrandPosts` (passed `brandRefCountInBg > 0` from the two main call sites ~3971/4064). When brand posts are attached, `colorLine` tells the model the SOURCE OF TRUTH for the palette is the brand-post reference images — sample their dominant recurring colours and grade the whole scene to match THOSE; the stored colour name is only a weak fallback the posts override. Job 227 confirmed: scene graded in Chili RED (from posts), ref person keeps her own clothing colour, cohesion held, no burned text. Cost 227=$0.054. Applies to ALL companies, not just 69.
- **Theme-scene must match the literal service + no duplicate logo (2026-07-08):** job 227 graded red correctly but put the ref person at a DJ/audio mixing console for a "gestão de redes sociais" ad — the themeScene generator free-associated 'conteúdo/redes' → audio studio. FIX (theme): the person-branch sceneSystem now (a) depicts the LITERAL work/deliverable of the exact service named (social-media mgmt → laptop with IG feed grid, analytics, content calendar), and (b) bans adjacent-industry clichés (DJ booth, audio/podcast/recording studio, soundboard, film set) unless the service is literally audio/video. Job 228 fixed the theme (marketing review meeting) but the model DREW the Chili logo on the laptop + TV slide (with bullets) = logo appeared 3× (1 composited + 2 burned). FIX (logo): sharpened the heroRef/ugcNoRef ZERO-TEXT rule — leads with '⛔⛔ NEVER DRAW THE BRAND LOGO — IT WOULD APPEAR TWICE', bans logo/slide/header/bullets on any screen/badge/lanyard, screens show only an abstract wavy line/glow. Job 229 confirmed: single logo, abstract screen, on-theme office, red brand devices. Costs ~$0.046 each. 2.5-flash-image burns logos/text on screen-heavy scenes — this is its known weakness.
- **Brand devices as an OVERLAY DESIGN LAYER + ref-payload-level bug (2026-07-08):** user wanted the brand-post design assets (bolinhas/formas) to show as a design LAYER of the ad, not just tint the photo background. FIX: `buildOverlayHtmlFromGemini` now takes `brandDevices` (passed `(heroRef||refAsSubject) && brandRefCountInBg>0`) and renders REQUIRED brand-colour CSS devices (dot-grid via radial-gradient, organic blob, outline ring, diagonal wedge) at z-index:15 (above scrim, below logo/text) in empty areas — with concrete copy-paste CSS snippets so the conservative 2.5-flash overlay model actually emits them. The image-model echo lines (heroRef/casting) were softened to NOT bake devices into the photo (keep it clean, leave empty areas for the layer). SEPARATELY discovered: my test script sent `reference_image` at the payload TOP LEVEL, but the endpoint reads `campaign.reference_image` — so jobs 225–233 ran with NO hero ref (Pexels/invented person, never matched the genius ref, varied every run). Moving it into `campaign` (job 234) featured the real ref woman + red blob devices visible in the corners. Cost with real ref ~$0.07 (3.5-flash interpret_image fires).
- **Brand-posts sliced out of the ref window + calm-zone revert (2026-07-08):** brand posts were APPENDED LAST in `generate-ads.php` allRefs, then the list is sliced to 4 (PHP) and to 3 (edge) — so a hero ref + product/company images pushed brand posts out of the kept window → some ads had ZERO brand-post influence (no devices, off-brand) intermittently (job 236). FIX: reorder allRefs to `[hero, brandPosts, ...product/company/bg]` so `composeCompanyRefs=[hero,bp1,bp2,bp3]` survives both slices; edge slice(0,3) then keeps [hero,bp1,bp2]. No edge change needed. Job 238 confirmed: strong red brand palette + Chili 'smile' device panel from posts. ALSO: reverted `pickCalmTextZone` model from gemini-2.5-flash BACK to gemini-3-pro-preview (2.5 read the calm zone poorly → text landed on the person/busy areas; user complained). ALSO: banned literal 'LOGO'/'YOUR LOGO HERE' placeholder wordmarks (2.5 burned a 'LOGO' placeholder on a wall, job 236). Ledger note: fire-and-forget logGeminiUsage drops mid-pipeline calls (3-pro calm-zone, 3.5-flash interpret_image often missing) → ledger UNDERCOUNTS; real ref-ad cost ~$0.07-0.09 not the ~$0.045 logged.
- **Design assets must be DETECTED from brand posts, not forced (2026-07-08):** user's principle — the design-asset layer (floating dots/shapes/blobs/icons) AND its colours must be read FROM the brand posts: if the brand decorates ALL its posts with graphic overlays → ours should too (same devices, same colours); if the brand's posts are clean → ours must NOT invent assets. My first pass FORCED assets whenever brand posts existed (wrong) and a CSS overlay device used a stored accent hex → rendered OFF-BRAND BLUE blobs (job 239). FIXES: (1) rewrote the heroRef/casting design-asset instruction to be CONDITIONAL + SELF-JUDGED — the image model reads the attached brand posts and only adds a matching design-asset layer IF the brand uses one, in the SAME devices + colours the posts use; else keeps the scene clean. (2) DISABLED the CSS overlay device (`brandDevices:false` at both call sites) — the overlay model can't see the posts so it can't match them (that was the blue source); design assets now come only from the image model (which sees the posts). (3) Bumped brand posts reaching the image model from 2→3 (hero+3=4 images: edge fetch slice 3→4, else-branch bgRefImages slice 3→4, briefDriven brandSlice 2→3) — 2 examples was too thin to detect/replicate the brand's design language (user asked 'é 3 pouco?' → yes, ~2 when a hero takes a slot). PHP composeCompanyRefs already caps at 4 = [hero,bp1,bp2,bp3]. Deployed edge; not yet visually verified.
- **Brand posts bumped 3→4 (2026-07-14, commit a97a1bd):** user found generated images too generic / weak on company identity ("imagens mt genéricas, pouca ref da empresa"), asked for +1 brand post. Raised the whole slice chain by 1 so the model gets hero + 4 brand posts (was hero + 3 = 4 imgs, now 5 imgs): `generate-ads.php` brandPostImages slice 3→4 (:362) + composeCompanyRefs cap 4→5 (:374); `agents-ads/index.ts` companyRefUrls fetch slice 4→5 (:3900), standard else-branch bgRefImages slice 4→5 (:3932), briefDriven brandSlice 2→3 (:3923). Deployed edge + PHP both servers. **User confirmed "ficou bem melhor"** — more brand-post examples visibly improved brand identity. Watch for 546 WORKER_RESOURCE_LIMIT (1 extra ref image per gen). If ever needed more, keep all 5 cut points in sync or the smallest wins.
- **Next run: use `testforge_job<id>` (test) / `live_job<id>` (live) naming (job id avoids overwrite). Next job id ~240.**

Update this note each time a run happens. Part of [[external-ads-api-test-procedure]].

- **Run 266 (2026-08-04, LIVE, job 297 / company 297 "Unica" #fee701):** verificação do fix de color-lock. Arquivos: `ad-tests/test266_instagram-square.jpg`, `test266_prompt_0.txt` (prompt de debug, 38k chars), `test266_status.json`. Payload mínimo (`company:{name,primary_color,accent_color}` + campaign genérica) — dados gravados da company são a BASE e só campos não-vazios sobrepõem, então não sobrescreve identidade. `debug:true` no body → `creatives[].debug.prompt` no job-status. Resultado: `Brand palette: amber;very dark amber;amber;...` (antes `;;;`), navy 0 / coral 0, brief-proxy com 8× `brand-coloured`, banner dominado por âmbar. Custo ~US$0,07. **Próximo run: 267.**
- **Runs 267-268 (2026-08-04, LIVE, company 297):** 267 (job 298) = fix de nome de cor (`#fee701` é AMARELO, não "amber"): fundo medido #DCB744, matiz 45° vs 55° alvo — família certa, ainda aproximado. 268 (job 299) = swatch da cor + remoção do nome da marca: partículas da marca em **#FAE10A vs #FEE701** (matiz 53.8 vs 54.6) — cor do payload atingida em elementos chapados; cena fotográfica graduada varia com luz por natureza. 6 imagens de ref enviadas (5 + swatch), **sem 546**. Nome da marca no prompt: 25 → 0. **Texto queimado AINDA acontece** (wordmark "abs" traçado dos brand posts + texto simulado em tela de laptop) — `VALIDATE_BACKGROUND=false` desde 08/07 por decisão de custo. **Próximo run: 269.**
- **Run 269 (2026-08-04, LIVE job 300, company 297):** modo proxy deixa de mandar os pixels do concorrente ao modelo de imagem. 6 refs → 2 (site do cliente + swatch). **ZERO texto queimado** (tela do monitor abstrata, parede com line-art sem letras), campo da marca #EDD803 = matiz 54.6 idêntico ao #FEE701, e o anúncio continuou rico/decorado — o brief em prosa sustentou a estrutura. Pendência: ainda saiu uma marca desenhada ao lado do logo real; fonte restante = a captura do site do cliente, que contém o logo. **Próximo run: 270.**
- **Runs 270-271 (2026-08-04, LIVE, company 297):** 270 = captura do site também sai do modelo em modo proxy, substituída por swatch de PALETA com bandas (topo = dominante). Refs: 1 (só o swatch). Fundo limpo de headline, MAS voltou com marcador + palavra literal "LOGO" **inventada** (não traçada — não havia imagem de onde copiar). 271 = removida a enumeração literal de `'LOGO'/'YOUR LOGO HERE'` do prompt: **zero placeholder, zero texto queimado**, tela do laptop com gráfico abstrato sem rótulos, decoração rica — melhor da série. Cor: campo chapado bate o alvo (#EDD803, matiz 54.6 no run 269); superfície fotográfica iluminada sai pastel (#EAD88E, matiz 48). **Próximo run: 272.**
