# Credenciais para o deploy manual (scripts/deploy-ftp.ps1).
# Copie este arquivo para `.deploy.env` (que e gitignorado) e preencha.
# Os valores sao os MESMOS dos GitHub Secrets FTP_USER / FTP_PASSWORD.

FTP_HOST=195.35.41.130
FTP_USER=seu_usuario_ftp
FTP_PASS=sua_senha_ftp
